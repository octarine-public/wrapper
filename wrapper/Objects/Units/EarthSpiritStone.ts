import { WrapperClass } from "../../Decorators"
import { Unit } from "../Base/Unit"

@WrapperClass("CDOTA_Unit_Earth_Spirit_Stone")
export class EarthSpiritStone extends Unit {
	// TODO: find a better way to do this & rename to npc_dota_(name)
	// public get Angles() {
	// 	// if (this.PredicitionAngles.IsZero())
	// 	return this.PredicitionAngles
	// }
}
