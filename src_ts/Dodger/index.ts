// import { EventsSDK, Unit, Menu } from "../wrapper/Imports";

// let all_units: Unit[] = []

// let MenuTree = Menu.AddEntry(["Utility", "Dodger"])
// let MenuState = MenuTree.AddToggle("State", true)

// EventsSDK.on("EntityCreated", x => {
// 	if (x instanceof Unit && x.IsHero) {
// 		all_units.push(x)
// 	}
// })

// EventsSDK.on("Tick", () => {
// 	if (!MenuState.value)
// 		return false;
	
	
// })