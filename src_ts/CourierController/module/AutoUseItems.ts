// import { allyCourier, EnemyHero, Sleep } from "../bootstrap"
// import { StateAutoUse, AutoUseItem } from "../Menu"
// import { CourierBase } from "../Data/Helper"

// // Todo for dagon and etc
// // let item = allyCourier.Items.find(item => AutoUseItem.IsEnabled(item.Name) && item.CanBeCasted())
// // if (item === undefined)
// // 	return false
// // let enemy = EnemyHero.find(x => x.IsAlive && x.IsEnemy() && x.IsVisible)
// // allyCourier.UseSmartAbility(item, enemy)
// // Sleep.Sleep(CourierBase.CastDelay)
// // return true

// export function AutoUseItems() {
// 	if (!StateAutoUse.value || allyCourier.Level < 25) {
// 		return false
// 	}
// 	let item = allyCourier.Items.find(item => AutoUseItem.IsEnabled(item.Name) && item.CanBeCasted())
// 	if (item === undefined)
// 		return false
// }
